--- a/net/minecraft/util/datafix/fixes/BlockStateData.java
+++ b/net/minecraft/util/datafix/fixes/BlockStateData.java
@@ -675,15 +_,15 @@
         "east", "true", "north", "true", "south", "true", "west", "true"
     );
 
-    public static Dynamic<?> create(String name) {
+    private static Dynamic<?> create(String name) {
         return new Dynamic<>(JavaOps.INSTANCE, Map.of("Name", name)).convert(NbtOps.INSTANCE);
     }
 
-    public static Dynamic<?> create(String name, Map<String, String> properties) {
+    private static Dynamic<?> create(String name, Map<String, String> properties) {
         return new Dynamic<>(JavaOps.INSTANCE, Map.of("Name", name, "Properties", properties)).convert(NbtOps.INSTANCE);
     }
 
-    public static void register(int id, Dynamic<?> tag, Dynamic<?>... legacy) {
+    private static void register(int id, Dynamic<?> tag, Dynamic<?>... legacy) {
         MAP[id] = tag;
         int blockId = id >> 4;
         if (BLOCK_DEFAULTS[blockId] == null) {
